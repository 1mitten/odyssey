# Research index

Every research result lives in this folder as one file per question, in the fixed format: Question, Findings, Recommendation, Sources, Confidence, Could not be determined. Findings only; sources are URLs. British English.

## Files

| File | Phase / lane | Status | Confidence | One-line result |
|---|---|---|---|---|
| `phase0-ground.md` | Phase 0 | done | high | Empty repository; no Unity, dotnet, git-lfs, CI, MCP or Synty content in the remote container. Unity-side spikes must run on a dev machine. |
| `synty-import.md` | Phase 0 | **done 2026-09-15** (Windows dev machine) | high | Pass with notes: Unity 6000.3.24f1 + URP 17.3.0, five packs imported headless under `Assets/Synty/` (7,222 assets), zero import errors, shaders SRP-native. Record and deviations in the file. |
| `synty-inventory.md`, `synty-inventory.csv` | Phase 0 | **generated 2026-09-15** by `Assets/Editor/Odyssey/SyntyInventory.cs` | high | 2,138 prefabs measured. Base walls 2.5 × 3.01 × 0.23 m, floors 2.5 × 2.5 m, Sections at exact 5 m multiples → cell **2.5 × 2.5 × 3.0 m, confirmed** (ADR 0002). Note: the generated "implied cell 0.00 m" line is the 80 %-snap heuristic failing across mixed packs; the histogram above it is the evidence. |
| `phase1-answers.md` | Phase 1 | **complete 2026-09-15** (interview conducted) | high | Cell 2.5 × 2.5 × 3.0 m confirmed; template stamping; IvanMurzak MCP now; pragmatic TDD; two-way architecture benchmark, determinism first; Windows primary. ADRs 0001–0002 in `docs/adr/`. |
| `unity-mcp-server.md` | Phase 1, Q4 | done | medium | Recommend IvanMurzak/Unity-MCP (Linux binaries, Unity 6000.3 named, tests, console, Roslyn C#). Runner-up CoplayDev/unity-mcp. |
| `a-01-pawns.md` | Phase 2, Lane A | **done 2026-09-15** | high | Full pawn model: needs as 0–1 scalars on a 150-tick interval with per-band rates; mood = difficulty base + Σ thoughts, drifting +12/−8 per hour; break thresholds 35 %/20 %/5 % with MTB 10/3/0.7 days; skills 0–20 with passion ×0.35/×1.0/×1.5, 4,000 XP/day soft cap and level-10+ decay; opinion = Σ social memories + relation offsets; tick buckets Normal/Rare 250/Long 2000. Only three environment queries (beauty scan, roofed check, room detection) need layer awareness. |
| `a-03-work-and-jobs.md` | Phase 2, Lane A | **done 2026-09-15** | medium-high | Think tree = first-valid-job ordered scan, utility only at three narrow points; WorkType→WorkGiver→Job→Toil shapes captured; reservations all-or-nothing pre-toil, released on any job end; constant tree every 30 ticks, main tree event-driven with expiry heartbeat. |
| `a-04-building-and-materials.md` | Phase 2, Lane A | **done 2026-09-15** | high (roofs, stuff, quality) / medium (frame internals) | Pipeline blueprint→frame→built thing, success roll and quality roll both at completion by the finisher; stuff formula `stat = base × factor + offset` verified against the wall table; roofs are massless annotations supported within 6 cells (thin collapse 15–30 crush at 0 % AP, thick obliterates); deconstruct refunds 50 %, repair is free, ruins claimable — Odyssey must promote roofs to material slabs with recursive bottom-up support. |
| `a-12-map-generation.md` | Phase 2, Lane A | **done 2026-09-15**; river/bridge open item closed 2026-09-16 by ADR 0009 | medium | RimWorld mapgen = ordered GenStep list (noise grids → terrain → rock → scatter passes → life → start); full translation table to districts, street-grid-first stamping, intactness grid, salvage lumps, utility taps and sealed vaults, plus a 40-layer strata proposal (service/metro/fill/rock). |
| `a-15-time-and-simulation.md` | Phase 2, Lane A | **done 2026-09-15** | high (constants) / medium (internals) | 60 ticks/s at ×1, speeds ×3/×6 (×15 dev); 2,500 ticks/hour, 60,000/day; tick groups Never/Normal/Rare 250/Long 2000 with hash-offset phase spreading, cost scales with ticking things not cells; RNG reseeded per tick from (world seed, tick) but vanilla is not lockstep-deterministic — Multiplayer mod's hazard list mapped as our determinism checklist; saves are between-tick XML via one expose pass with load-ID cross-references, mid-job driver state persists. |
| `b-going-medieval.md` | Phase 2, Lane B wave 1 | **done 2026-09-15** | medium-high | Voxel Unity colony sim; integer stability radius (4, −1/step, beams extend); 0.5-step cut-away whose ghosted-but-clickable layers are the top player complaint; per-room temperature where volume and materials beat depth; verticality itself the top praise. |
| `c-cataclysm-dda.md` | Phase 2, Lane C wave 1 | **done 2026-09-15** | high (architecture) / medium (cost figures) | Reality bubble = 11×11×21 grid of 12×12-tile submaps with lazy per-level caches; 3D recursive shadowcasting (~80× 2D cost, capped by a z-range option); one A* with per-level scratch layers but stairs are run-time-matched teleports; ten-year retrofit (optional 2015→mandatory 2020→3D FoV default 2024→3D mapgen 2024) — the dual-mode era, per-storey mapgen and stair matching are the pains Odyssey designs away. |
| `e-04-tint-strategy.md` | Phase 2, Lane E | **done 2026-09-15** | high (local) / medium-high (instancing) | Shared UV0 atlas, no vertex colours; `Generic_Basic` already multiplies `_BaseColor` over the atlas (all props in the SRP Batcher CBUFFER, none per-instance). Commit: one cached material per stuff + `RenderMeshInstanced` buckets per (mesh, stuff) — stock shaders untouched; fallback: own-authored Hybrid-Per-Instance tint graph + BatchRendererGroup. Never fork Synty shader files out of `Assets/Synty/`. |
| `e-05-character-customisation.md` | owner question, 2026-09-16 | **done 2026-09-16** | high (measured on disk) | No generic Synty body exists and nothing below the neck is separable: a character is one skinned mesh with one material, 69 of them on one shared 49/50-bone Humanoid rig. Sidekick is the only route to real modular garments and the owner declined it. **But the atlas is a labelled `Character Colours` palette of flat swatch cells, so a vertex's UV already is its material identity** — measured flatness 0 on every cluster of eight bodies across all four packs. Built: our own shader replacing colour inside UV rectangles, rectangles discovered by clustering at catalogue-build time (55 of 61 bodies Full, every body recolours its clothing), one material per look, appearance derived from the world seed. Skin is painted in three different places, not one. |
| `e-06-modular-colonists.md` | Phase 4, modular colonists | **done 2026-09-22** | high (measured) | Battle Royale is one rig with fifteen sibling bodies and **rigid** bone-parented hair and beards; PolygonGeneric ships the same shape, so **one mechanism serves both packs**. Prefabs import clean, the humanoid bone maps are identical (40 each), heights match to 0.3% so `scale 1.4` is untouched, and the swatch classifier reports `Full` on 14 of 15 bodies unmodified. **Decisive: most hair and beard meshes map every vertex to the single atlas texel the scalp already uses, so repainting the hair rectangle recolours scalp, hair and beard together for nothing** — which falsifies the pre-measurement belief that hair colour would need a second mechanism. Six pieces span real texture and must be excluded, including **both** of BR's named female hairs. The far form must draw attachments as instanced modules, never baked into the body mesh. |
| `a-14-bills-stockpiles-inventory.md` | Phase 2, Lane A | **done 2026-09-15** | high (fields, priorities, bill modes) / low (tick cadence) | Haul destination = filter accepts → has space → highest priority → nearest; priority orders the destination, not the queue. One filter record shared by storage settings and bill ingredients. Bill state machine Created→Active with Suspended/Paused/Completed. **Layer question 6 answered: zones are per layer**, with named storage groups sharing one settings record across layers. |
| `a-07-power-and-networks.md` | Phase 2, Lane A | **done 2026-09-23** | high (numbers, adjacency, batteries) / low (shed order, overlay trigger) | Nets, the per-tick balance, the wood-fired generator, heater and stove, conduits, short circuits. **Backs the owner's power decisions** (design 32): burning in proportion to load, vertical 6-neighbour conduits and whole-net-dark all depart from the reference, and whole-net-dark answers its flicker complaint provided demand counts consumers switched on, not consumers powered. Generator 1,000 W, 75-wood hopper, 22 wood/day; heater 175 W. |
| `a-08-plants-growing-food.md` | Phase 2, Lane A | **done 2026-09-16** (OQ-29); **zone-loop follow-up 2026-09-18** | high (growth formula, species figures, hunger, rot, hydroponics) / medium (felling = harvest work; per-tile nutrition derived) | Growth = fertility × temperature × light factors on a 13-hour growing day (real days ≈ 1.85 × listed); **felling should be 800 ticks and 27 wood** (pine class, replacing the 600/20 ASSUMED in `PawnContent.cs`), scaled by plant work speed (8% + 11.5%/level) once skills land. Humans burn 1.6 nutrition/day, a meal is 0.9, starvation from full takes 72.5 h; meals rot in 4 days above 0 °C, survival rations never. **The scenario's ration pack is worth half a vanilla meal** (450 of 900 units), which is why five colonists eat 15 a day; at 900 a ten-day run needs ≈ 90. A self-feeding colony needs ≈ 16.5 soil tiles or 1.5 basins of rice-class crop per colonist plus a six-day pantry. The follow-up: growing zones are painted continuous loops — sowing repeats itself, clearing is folded into sow work, growth needs >51% light and the siting gate is minimum fertility. |
| `a-02-health.md` | Phase 2, Lane A | **done 2026-09-16** (OQ-27) | high (parts, capacities, pain, bleeding, healing, tending, downed/dead) / recommendation for the fall table | Part tree with HP and capacity links; pain 1.25%/HP, shock at 80%; any tend stops bleeding, blood recovers 33%/day; heal rate 8 + 8 bed + 4–12 tend HP/day; tend quality = skill × potency, clamped 70/100/130% by medicine. **Downed** = consciousness < 30% or pain ≥ 80% or moving ≤ 15%; **dead** = vital part gone, vital capacity at 0, blood loss 100% or 150 HP of damage. Vanilla never drops a pawn, so U29's fall rule is calibrated on thin-roof crush (15–30), pain shock and the 150 HP threshold against the trauma LD50 of 4–5 storeys: **`fallDamage = 15 × layers^1.5` blunt on the bottom-facing parts** — one layer bruises, three down, five kill. Tend duration not found. |
| `d-01-architecture.md` + `d-01-bench/` | Phase 2, Lane D1 | **complete 2026-09-15** | high | Both candidates implemented against a pinned workload and **produced the identical state hash** — the equivalence gate that makes the timings comparable. Plain **1.423 ms/tick** vs ECS **2.446 ms** (1.72×), zero allocation in both, view build 0.186 ms against its 0.8 ms budget. **Decision: plain C# SoA with Burst on hot paths (ADR 0005).** Standing finding: phase 3 pathfinding is 65% of the tick and 1,058 of 1,800 replans exhausted their budget on unreachable targets — the evidence behind D4, and the project's top performance risk. |
| `d-04-pathfinding.md` | Phase 2, Lane D4 | **done 2026-09-15** | high (architecture, determinism) / medium (numbers) | Chunk-local typed regions (10×10×1, never spanning layers), boundary-span links, per-traverse-mode **district ids** so `Reachable()` is one integer compare. Rooms are a *separate* atmosphere grouping over the same regions. Two-stage abstract-then-cell A-star (Factorio shape + HPA caching); JPS and D* Lite rejected with reasons. Connectors declare both ends — the opposite of CDDA run-time stair matching. Integer-only costs, total order `(f, h, cellIndex)`, budgets in nodes not milliseconds. |
| `d-06-save-load.md` | Phase 2, Lane D6 | **done 2026-09-15** | high (libraries, oracle) / medium (size maths) | Binary container + MemoryPack + per-section LZ4. Palette bit-packed 25×25×5 chunks put the 2.5M-cell grid at **0.31 MiB** (15× smaller than plain binary, 85× smaller than per-cell XML); whole save ~1.6 MiB compressed, so **post-load re-derivation is the load budget, not parsing**. Four-phase reference resolve; mid-job state persists, paths are recomputed. Resume-equivalence *is* the M0 headless gate. Risk: MemoryPack under IL2CPP unverified. |
| `d-07-data-pipeline.md` | Phase 2, Lane D7 | **done 2026-09-15** | high (shape, tie-break, pass order) / medium (hot reload) | **XML committed**, and the tie-break is the path language: XPath addresses by predicate, JSON Patch addresses array elements by index and breaks when a second mod inserts earlier. Ten-pass loader with **patch before inherit**; zero-match XPath is an error by default; Roslyn source-generated binders (AOT-safe, no reflection); integer `DefHandle<T>` in hot paths; three-tier hot reload with an atomic tick-boundary swap. Risk: `System.Xml.XPath` under IL2CPP unverified. |
| `d-08-ci-tooling.md` | Phase 2, Lane D8 | **done 2026-09-15** | high (licence facts, recommendation) | **Unity Personal can no longer do manual/offline activation** — the `.alf`/`.ulf` route was withdrawn for Personal and the remaining flags are paid-tier. Therefore: self-hosted runner on the already-Hub-activated dev machine, no secret and no terms grey area, with a warm `Library/`. game-ci `unity-test-runner@v4` stays the default for any hosted parity job. Full workflow YAML in the file; branch protection is the step that turns a report into a gate. |
| `d-03-rendering.md` | Phase 2, Lane D3 | **visual half done 2026-09-15**; performance half pending | high (visual) | `VisualBlockScene.cs` generates `Assets/Scenes/Spikes/VisualBlock.unity`: intact + ruined shells from the Base kit on the exact grid, skyline, street life, FX — the look-check for the MVP. Stair pivots need special-casing; destroyed variants sell the ruin unaided. |
| `e-01-module-mapping.md` | Phase 2, Lane E | **done 2026-09-15** | high | The buildable kit is PolygonGeneric `SM_Bld_Base_*` (walls 2.5 × 3.01, floors 2.5 × 2.5, stairs rise 1.5 m per cell — two-cell stair rule is native); Sci-Fi City supplies 5 m facade Sections and props. Gaps: bed (Farm `SM_Prop_Bed_01`), lamp/battery/solar (Blender), generator (`SM_Bld_Power_01`). |
| `e-02-characters-animation.md` | Phase 2, Lane E | **done 2026-09-15** | high | Pawns on the Polygon ~50-bone rig (Sci-Fi City + Generic prefabs, one controller); Base Locomotion covers idle/walk only (2 of 8 slice clips); carry, mine, build, sleep, eat, downed all via Mixamo humanoid retarget; no ladder-climb clip exists. |
| `e-03-other-packs.md` | Phase 2, Lane E | **done 2026-09-15** | high (enumeration) / medium (style, URP FX) | Farm: ~20 staged crop species + wheat cycle + 5 × 5 m dirt-row tiles (= 2 × 2 cells); **no animals in any pack**; no stove anywhere — cooking is WF campfire then a Sci-Fi City appliance; Particle FX covers all M4/weather events (legacy particle shaders need a URP check); exclude adobe/teepee/period Americana, keep scrap timber as on-theme salvage. |
| `f-prior-art-here.md` | Phase 2, Lane F wave 1 | **done 2026-09-15** | high (repo) / medium (Ramble picks) | All Phase 0–1 artefacts carry into Phase 3 (verdict table in file). *Ramble* found in full at `D:\code\ramble` (active, Godot 4.7 first-person survival — not top-down); nine design-intent carry-forwards, no code ever. `CLAUDE.md`/`README.md` status sections flagged stale. |

| `g-01-ui-information-design.md` | Lane G (UI) | done 2026-09-15 | medium | Reference UI taxonomy region by region, five density techniques, what to reject from an immediate-mode implementation, and the Depth Ruler as the region with no counterpart. |
| `g-02-unity-ui-framework.md` | Lane G (UI) | done 2026-09-15 | medium-high on the choice, low on the numbers | UI Toolkit behind a Unity-free HUD assembly, with three flip conditions and twelve unrun experiments. Its dynamic-atlas assumptions were later corrected against the engine source: see `docs/adr/0007-pixel-art-icon-pipeline.md`. |
| `g-03-roster-paging-and-reorder.md` | Lane G (UI) | **done 2026-09-18** | high | Roster pagination, right-click drag-and-drop slot swapping (A ↔ B) with pointer capture and zero-GC slot coordinate arithmetic, mouse wheel page cycling, cross-page drag hover, and auto-page focus on alerts/colonist selection. |
| `d-09-stylised-rendering.md` | Lane D (Unity) | done 2026-09-16 | medium-high | Which techniques make low-poly read as hand-drawn, each weighed against instanced submission and the atlas. **Backs depth-reconstructed normals inside the existing outline pass** — crease lines with no prepass. Corrects two comments in our code: the Synty shaders *do* have DepthNormals passes, and banded lighting would not need per-material edits because `MaterialCache` already builds every world material at runtime. Object-ID outlines are effectively unavailable in URP 17. Rules out inverted hull, geometry shaders, Kuwahara, cross-hatching, with reasons. |
| `d-10-outline-pass.md` | Lane D (Unity) | done 2026-09-16 | high on the API, medium on the fix | The Render Graph idiom for a fullscreen pass, and the thin-geometry problem. Verified in the shipped package that **every material overload of `AddBlitPass` is an unsafe pass**; `AddRasterRenderPass` + declared `UseTexture` is what URP's own feature uses. Backs a **one-sided ink plus a "sliver test"** — suppress the line on features narrower than the sample diameter — because the question is how wide a thing is on screen, not how far away. Implemented 2026-09-16. |
| `a-05-rooms-and-beauty.md` | Phase 2, Lane A | **done 2026-09-17** (OQ-28) | high (formulas, thresholds, the layer answer) / medium-high (detection architecture) | Room detection, roles, impressiveness, beauty and cleanliness, with the numeric tiers. **Layer question 3, rooms half: per layer, and now evidenced rather than assumed** — Dwarf Fortress states a room cannot span z-levels and Going Medieval independently treats each storey as its own. The regions-as-substrate separation is not merely compatible with the reference design, it **is** that design; the structural price is that a portal cell must be its own region or the room layer has no boundary to split on. Build the typed cross-layer **opening** graph at M3 beside detection, since temperature is its first consumer. Two traps: "enclosed" is three booleans with three deliberately disagreeing thresholds, and the impressiveness formula's `ln` is a **cross-runtime hash hazard** — quantise to an integer tier before anything simulated reads it. |
| `a-06-temperature-environment.md` | Phase 2, Lane A | **done 2026-09-17** (OQ-30) | high (published cadences, ONI equation, light bands) / medium (inferred wall law) / medium-low (Going Medieval) | Heat sources, insulation, equalisation, seasonal outdoor temperature, light and glow. **Layer question 3, heat half: heat rises, and it needs no buoyancy** — rooms stay per layer, and a vertical opening is an ordinary surface whose conductance depends on the sign of the difference, about 4:1 up against down. That is the whole of buoyancy as a player can tell. Going Medieval took the other branch, merging levels through a stair and averaging, and its own players report the failure that predicts: a tall stack loses its heat and loft and cellar read the same. Unlooked-for win: a depth-indexed ground boundary means solid rock needs no temperature at all. |
| `a-16-modding-architecture.md` | Phase 2, Lane A | **done 2026-09-17** (OQ-26) | high (Def pipeline, Harmony) / medium-high (what broke in the layer mods) / low (their internals, performance) | Backs **data-and-registry modding**: unified-document Def pipeline, one sparse-aspect extension generalised from `PawnAspect`, self-registering everything — and explicitly declines to promise IL patchability of the sim, because Burst and SoA destroy the seams ADR 0005 chose. **The finding that reframes the vertical slice:** every generation of multi-level mod succeeded at *traversal* and failed at *queries*. Pawns walked between levels early; bills, hauling, construction delivery, the right-click menu and reachability stayed same-map, bridged by a hand-made per-bench link. None ever delivered cross-layer line of sight or combat. A colonist climbing a ladder proves nothing; **a bill sourcing ingredients two layers down is the test.** Confirms d-07: patch before inherit, and a zero-match XPath must be an error. |
| `b-dwarf-fortress.md` | Phase 2, Lane B | **done 2026-09-17** (OQ-31) | high (data model, designations, ramps, the Steam UI list) / medium (pathing cost shares) / low (fog parameters) | The transferable idea is the **two-part cell** — a volume plus the seal at its bottom — which makes dig-down, build-up, falling and "is this roofed" one question. The transferable failures are per-cell vertical structures nobody validates, a depth fog tuned for outdoors, and an alert popup where a feed belongs. DF's cell is ~2 × 2 × 3 m, within a few per cent of ADR 0002 and arrived at independently twenty years earlier; Tarn Adams names the 2008 2D-to-3D map conversion as the debt he cannot pay, which argues for settling our deck and chunk-shape questions **before** M4. Carries an editor's note: its ~6% A-star figure is a running fortress and our 65% is the D1 stress harness, so the two are not the same measurement. |
| `b-timberborn.md` | Phase 2, Lane B | **done 2026-09-17** (OQ-32) | high (representation, support rules, water, the UI complaints) / medium (layer widget form) | **Not a counter-example to ADR 0002** — its terrain is cubes too, with no sloped geometry, so the row's premise was wrong. The real difference: their transition piece is a def'd 1 × 1 × 1 **object in a cell** — non-solid, floodable, selectable, tunable as content — where ours is a drawing offset priced in three separate seams that our own notes say fail silently when they disagree. Verdict: keep the cell size, amend the scope. Two warnings: their terrain was a **heightmap for four years** and true 3D took a flagship update to retrofit, so audit any subsystem that assumes one surface per column; and their top layer-UI complaint is a forgotten filter that reads as a bug, while their longest-standing unmet request is simply a persistent cursor elevation readout. |
| `b-stonehearth.md` | Phase 2, Lane B | **done 2026-09-17** (OQ-33) | high (architecture, AI design, ACE's Lua-only scope) / medium (pathfinder) / low (memory and GC folklore) | The post-mortem. The killer was not "Lua is slow": **deciding required searching** — the HTN fanned every task to every hearthling and each think ran item and pathfinder queries, on one thread, with a thread-based rescue believed in and never built. CPU starvation presented as AI stupidity and was misdiagnosed as an AI bug for years. Largest *measured* cost was a C++ item-filter cache invalidated on every item move, 15–18% of tick, scaling with **items and containers rather than agents** — which indicts our benchmarks, since everything we measure scales pawns and map size. The early warning to watch is **pathfinder calls per job assigned**, not frame time. Marks each paragraph as developer statement, measurement or fan speculation, and cites one top search result as a *negative* source: a post the community flagged as fabricated whose invented claims search engines now repeat. |
| `c-goblin-camp.md` | Phase 2, Lane C | **done 2026-09-17** (OQ-34) | high (licence, status, mechanisms read from source) / medium (design intent) / low (play experience, scale) | **GPLv3, Ilkka Halila, dead since 0.21 in 2012, archived read-only 2023** — licence first, and the file states that a permissive licence would not change our rule: read for technique, never copy. Technique worth having: job as a step list with job-owned reservations released on one path, four priority bands with a fifth waiting list, stockpiles that **grow into adjacent legal ground instead of reporting themselves full**, and a spot reservation that immediately moves the destination's stock counts so in-flight goods are counted — their own fix for the everyone-hauls-to-one-pile stampede. Dispatch is a **global optimal assignment** (Hungarian, capped at twenty jobs a pass because larger matrices were too slow), not a per-pawn search. **Do not copy their distance:** straight-line, which in a layered world is wrong in kind — an unreachable pile can rank nearest. |
| `b-painted-look-games.md` | Lane B (prior art) | done 2026-09-16 | high on Xrd, medium on the rest | How shipped painted-looking games do it. Guilty Gear Xrd is the only fully documented pipeline and chose inverted hull *over* screen-space — which we cannot afford. Sable fades outline opacity with distance, the idea our shader already had. The useful negative result is a Ghibli-style breakdown achieving it with **no outlines and no cel shading at all**. Against the Storm, Timberborn and Dorfromantik have published nothing technical — recorded so nobody searches again. |
| `d-11-instanced-submission.md` | Lane D (Unity) | done 2026-09-16 | medium on the API, low on the cause | Why `RenderMeshInstanced` might go superlinear, and the sanctioned fast paths. Nothing in Unity's documentation predicts superlinear cost; the API throws rather than silently falling back to one draw per instance. Best explanation offered is a harness artefact: a `camera.Render()` loop never Presents. **Backs BatchRendererGroup** as the destination for cached per-chunk matrices (Synty materials already carry the DOTS-instancing variant), gated on first proving the pathology in a real player loop. |
| `look-interview.md` | Owner interview (feature) | **complete 2026-09-16** | high | The golden-hour look settled in ten answers: warm sky-into-fog, our own sun rays, a subtle always-on tilt-shift, bloom, grading and vignette, fixed golden hour, about 2 ms with a Low tier, and the camera allowed to pitch down to 15–20° so the horizon is in frame. Two recorded decisions are overridden by the owner: the **72° sun comes down to a raking 25–35°** (`PlayScene.BuildLighting`'s comment), and **bloom is adopted** against `d-09` §3.4's caution. Grounding's headline: there is **no volume stack in effect at all** — the URP asset's default profile GUID resolves to nothing and `DefaultVolumeProfile.asset` is orphaned, so no tonemapping, grading, bloom, DoF, vignette or anti-aliasing has ever run. |
| `d-12-urp-post-stack.md` | Lane D (Unity) | **done 2026-09-16** | medium-high on configuration, low on cost | **Only Bokeh can give a tilt-shift band** — Gaussian blurs the far field only, so the near half of a pitched frame never blurs. Bokeh needs exaggerated optics (200 mm, f/2.0) because honest values give zero blur at board distances, and Focus Distance should be driven from code as camera-to-ground at screen centre. A third option is named and may beat both: **a fullscreen pass blurring by screen Y**, exact, no depth texture, scene-independent cost. **The whole golden-hour grade is free** — tonemapping, colour adjustments, white balance, SMH, split toning, curves and LUTs bake into one resolution-independent LUT; vignette, grain and FXAA are per-pixel inside UberPost; only bloom and DoF are real passes. **Neutral, not ACES** (ACES desaturates and hue-skews exactly the warm highlights the look needs) and **HDR grading is not optional** (in LDR the sun clips before the grade sees it). Bloom's dominant lever is Downscale, not iterations. **SMAA at 1080p**, because FXAA destroys a one-pixel post-drawn outline. Tier mechanism is a URP asset plus a generated VolumeProfile per quality level; trap recorded: use `volume.profile`, never `sharedProfile`, or a runtime override is written to the asset on disk. **No measured millisecond figure for any URP post effect exists in any source** — Unity publishes none. |
| `d-13-light-shafts.md` | Lane D (Unity) | **done 2026-09-16** | high on the algorithm and the API, low on cost | GPU Gems 3 ch. 13 verbatim: radial blur of a depth-built occlusion mask centred on the sun's screen position, with density, decay, weight and exposure. Commits to **half-res R8 mask, three 12-tap blur passes ping-ponging two half-res textures, full-res Screen composite, injected at `AfterRenderingSkybox` (400)** so the glow sits under our outline (450). Confirms `d-10`: material `AddBlitPass` overloads are unsafe passes, use `AddRasterRenderPass` + `UseTexture`, exactly as `OutlineFeature` does. Two corrections to the brief's assumptions: the manual says **avoid `CreateRenderGraphTexture`**, derive a `TextureDesc` from the active colour descriptor instead; and per-camera values must go in `PassData` with `SetGlobalVector` inside the render function, never on the shared material, or a second camera overwrites the first. Off-screen sun is a **hard cut on `clip.w <= 0`** plus a guard-band smoothstep, never a clamp (a clamped sun pivots along the screen edge and reads as a bug). Build the mask from **depth only**, not scene colour, or fog brightens distant occluders until they stop occluding. **The gating caveat: at the 48° camera pitch with the 72° sun the sun is about 120° off the view direction — behind the camera, effect permanently off.** Run the framing experiment before building anything. |
| `d-14-aerial-perspective.md` | Lane D (Unity) | **done 2026-09-16** | medium-high | **Built-in exp2 fog plus an agreeing gradient sky; no fullscreen pass.** The curve choice is decidable rather than taste: at density 0.0021 fog is 1% at 50 m, 20% at 224 m, 32% at the rim and 97% at 900 m — gentle inside the board, dissolved in the surround — where plain exponential puts 17.6% on the nearest cells and the existing 460→1,100 linear pair reaches only 24% at the rim and should be deleted. **The 50–224 m visible band and the 48 m board height kill the case for height fog**: the camera geometry suppresses exactly what a fullscreen pass would add, and that pass costs about 0.34 ms at 1080p plus a colour copy, a third of the meadow frame. **The sky is not fogged** — set the sky's horizon colour exactly equal to the fog colour from one place in C# and the join is invisible by construction. A warm sun lobe in the background pass is four instructions and only pays at shallow pitches. Double-fogging has a clean fix (`RenderSettings.fog = false` neuters `MixFog` project-wide) and shaders are never edited for it. Two jobs in the same change: audit ground, skirt, grass and chip materials for the fog pragma, and check the surround tiles for per-vertex fog banding at 20° and 14°. |
| `d-15-water-body-rendering.md` | Lane D (Unity) | **done, and built, 2026-09-17** | high (cause) / medium (the falls face) | **The generator is innocent and it was measured: 0 of 605/713/689 water cells float on three seeds, and the stream never drops more than one layer between adjacent columns — both ADR 0009 invariants hold exactly.** The fault is one line: non-solid terrain resolves to `ModuleShape.FloorSlab`, so water is drawn as a flat **lid with no sides**, raised to 0.72 of its cell — a horizontal tile hanging **2.16 m above its own bed** with open air between. That single fact gives all three symptoms: the void is in plain view wherever a neighbour is lower (27 cells a board), a one-layer cascade step (44 pairs a board) is two shelves 3 m apart with a dry face between them, and several depth-less lids in the below-slice band blend into the murky wedge. **Backs one skirt with two triggers** — a vertical water face wherever the neighbour is not water at the same level, dropped to the bed or to the water below — which is the existing `GroundFace` idiom, not a new one. Stop-gap: `WaterSurface` 0.72 → 1.0 hides most of it for one number, and doubles as the experiment that settles whether the wedge has a second cause in the transparent draw. Rejects flattening the beds in worldgen (fixing a drawing fault in the simulation), shader-only depth (a hole is not a shading problem) and a two-layer deep core (ADR 0009 decision 4). **Built the same day**: `WaterMesh` (two sheets), `ModuleShape.WaterSurface`/`WaterFall`, faces emitted by `WaterContributor`, and the shader's `clip(normalWS.y - 0.5)` deleted — it would have discarded every face silently. 44 faces on seed 1, all cascades; the lip branch is correct and unreachable until somebody mines beside water. **And the owner's diagonal wedge turned out not to be water at all**: it is the bank ramps, proved by shooting the same frame with `BankLayout.Enabled = false`, and is left unfixed on purpose. |
| `b-station-to-station.md` | Lane B (prior art) | **done 2026-09-16** | low on technique, medium on design intent | **A negative result first: there is no public technical account of that game's rendering** — no case study, no talk, no art breakdown, and the founder's 100-post dev blog with a lighting label contains nothing about it. **"Unity" is probably the wrong guess** (the studio advertises C++ and Unreal roles; the founder's background is an in-house C++ engine), so the game must never be cited as Unity evidence. What *is* sourced is the design intent, and it is the prize: the look is styled after **a physical diorama**, specifically a theme-park model railway, and the publisher's own copy calls it "tilt shift-inspired". The one hard developer rule found is about readability — buildings are vetoed on "is it easy to read from a distance?" before art quality, and good designs were axed for reading badly. Inferred from the images, not sourced: a low warm sun at 15–25°, crisp contact shadows, and a heavily **lifted blue-violet shadow** (large hue gap, small value gap); a blur band that tracks **screen position rather than depth**; moderate bloom, mild vignette, warm grade, no grain. Flags a tension worth the owner's eye: ground relief measured its 0.35 m amplitude as invisible *against a 72° sun*, so relief and a low sun are the same feature, and the cheapest experiment is one `ReliefCheck` pair at 72° and 20°. |
| `b-low-sun-readability.md` | Lane B (prior art) | **done 2026-09-16** | medium | **The headline is a bug in the current setup, not a setting: cascade splits are fractions of camera distance, and the nearest visible ground is 50 m away, so the stock splits spend half the shadow atlas on empty air in front of the camera.** Shadow length is `height / tan(elevation)`, so at 30° a 12 m tree throws 21 m (eight cells) and a colonist throws more than a whole cell — the board is mostly shadow, and the shadows must therefore be made *light* rather than few. **Shadow Strength 0.6 is the answer to why the 72° sun was chosen**: the old sun fixed the symptom (fewer shadows) at the cost of the look, strength fixes the cause (shadows too dark) and costs nothing. Pair it with gradient ambient (cool sky, warm ground bounce), Neutral tonemapping, contrast slightly down and no SSAO. **Sun azimuth 40° off camera forward and world-fixed**, so shadows rake across rather than towards the viewer and do not rotate with the camera. Start at **250 m shadow distance, 4 cascades at 0.30/0.48/0.70/1.00, 2048, soft medium, depth bias 0.5 / normal bias 1.4** — normal bias, not depth bias, is the grazing-angle lever, because depth bias peter-pans a long way when the light is shallow. **Do not drop to 2 cascades** (URP appears to give both counts the same tile, so 2 spend it on twice the range — the one load-bearing claim needing Frame Debugger verification). No second fill light under Forward+; gradient ambient plus shadow strength buys the same thing free. Estimated +0.3–0.8 ms for 50 → 250 m, to be measured. |
| `mining-interview.md` | Owner interview (feature) | **complete 2026-09-16** | high | Mining MVP settled in sixteen answers: strata + outcrops everywhere (no mine site), stone/iron/coal, sealed caverns found by digging, ore hidden until a face is exposed, flat palette with an emissive seam, the whole loop end to end on the wooded board. Two answers were forced by code findings — the 16-layer board is too shallow for coal (ground rises 6 → 10), and a mined shaft strands a colonist because vertical movement is only ever a declared connector (so a dug shaft leaves a ladder). |
| `work-speed-and-stats.md` | Owner question, 2026-09-17 | **done 2026-09-17** | high on the curves and composition / medium on the mechanism | How a skill level becomes a *rate*. Every work-speed stat is **dead linear in the level with no diminishing returns**, and every slope is chosen so **level 8 reads exactly 100%** — the reference's statement about who an average colonist is. Mining 4% + 12%/level is the steepest curve in the game (61× novice to master), construction 30% + 8.75% the shallowest (6.8×, because there skill buys quality and a lower failure rate instead of throughput), plant work 8% + 11.5% with a 10% floor, and **hauling has no skill speed at all**. Composition is base → additive offsets → multiplicative factors → clamp, with capacities *weighted* by how much a job depends on them (manipulation full and uncapped, sight partial and capped at 100%). Work is a **counter in ticks-at-standard-rate incremented by the finalised speed**, never a total divided up front, which is what lets a rate change mid-job land correctly. Two findings that change our design: it **contradicts `a-04`'s construction figure** (50% + 15/level, which would break the level-8 invariant), and **the mood-driven work-speed bonus was deliberately removed** — mood should produce visible events, not an invisible percentage tax. **Follow-up the same day** — does condition slow a colonist, and by what path? Hunger yes and by the predicted path (at 0% food it stops being a need and becomes a *condition with a severity bar* that offsets **consciousness** by −10/−20/−30%, and consciousness feeds both moving and manipulation, so one offset lands on both halves of a job); **exhaustion no, not at all** — tiredness touches mood and disease immunity only, and at zero rest the colonist collapses and sleeps where it stands. So it is one philosophy applied three times: **a condition either does nothing to your rate or produces a visible discrete event, never an invisible percentage tax.** Feeds `docs/design/17-rates-and-stats.md`. |
| `d-12-audio.md` | Lane D (Unity) | done 2026-09-16 | medium-high | Unity's audio best practices weighed against this game's camera and architecture: mixers are the mix *concept* set but have no supported creation API, so **buses live in code with the mixer's dB math** and the asset is the named migration target; pooled voices culled before they are spent; custom rolloff because logarithmic never silences; **ambience measured around the camera's focus** (one bed per environment, centroid-placed, layer-aware) rather than a source per cell; clip import classes per the manual. Backs ADR 0010 and the implementation in `Assets/Odyssey/Presentation/Audio/`. |
| `beds-interview.md` | Feature: beds (ground + interview) | **complete 2026-09-17** (round 1, four answers) | high (code read on `main`, inventory measured) | A "bed" already exists as an invisible scenario cell with a 100/80 rest split and a sleep chooser; the build pipeline is generic over the thing built, so a bed site flows through hauling and building untouched. Nothing exists for the rest: no multi-cell edifice (stairs are two paired records), no rotation input (R is slice-up), no ownership state (reservations are transient), no quality field (U26's outstanding success roll). No Synty asset spans two cells at bed proportions — Farm `SM_Prop_Bed_01` (2.08 × 2.67 m) is the one-cell module e-01 already chose; a true 2-tile bed needs new or derived art. **Answers: 2 × 1 cells with computed placeholder art; R rotates only while a rotatable ghost is armed; owner assigned from the bed's pane via popover; the five quality tiers scale rest effectiveness now with provisional XML numbers.** **Design: `docs/design/20-beds.md`.** |
| `a-11-storyteller-incidents.md` | Phase 2, Lane A, item 11 (asked 2026-09-20 for the events line) | done | medium-high | Incidents are a category then a weighted pick; storytellers are a difficulty curve plus independent generators (mean-time-between, on/off cycle, random-main, triggered); a Def is gates plus a worker with a `CanFireNow` / `TryExecute` split; letters have a kind, a look-target and an archive; cargo pods are a skyfaller. Nothing fires on a calendar date — "weekly" has no counterpart. Reward-on-completion is a quest, not an incident. |
| `e-06-head-look-kinematics.md` | Lane E (Synty fit) | **done 2026-09-18** | high | Procedural head/neck look-at kinematics on PlayableGraph without OnAnimatorIK: 30% Neck / 70% Head distribution prevents Synty low-poly pinching; reference frame from Chest bone; yaw clamped ±60° with cubic rear falloff 60°→95°, pitch −40°..+35°; `SmoothDampAngle` (smoothTime 0.12 s, maxSpeed 300°/s) produces natural human saccade profile; 0 B GC.Alloc struct state on `Figure`. |
| `a-17-ambient-gaze-and-motion.md` | Phase 2, Lane A | **done 2026-09-18** | high | 6-tier gaze priority arbiter (SleepLock > LadderTraversal > WorkFocus > SocialPassing > AmbientWander > PathForward); zero-allocation presentation-side social greeting check ($N \le 64$ flat Chebyshev filter < 6 m, dot product > 0.42); 3–6 s dwell / 1–2 s glance / 0.22 s smooth damp saccade; verticality rules for ladders (climbing up +30°, down −35°, yaw ±10°) and sleep/hauling. |
| `e-07-head-axial-rotation-and-felling-gaze.md` | Lane E (Synty fit) | **done 2026-09-18** | high | Pure Head-only cervical axial rotation (0% Neck, 100% Head) around `neck.up` (Y) and transverse condyle axis `neck.right` (X) eliminating ear-to-shoulder roll; +1.30m work target elevation for tree felling and rock mining leveling gaze to trunk notch; idempotent post-evaluate transform application eliminating in-place Lie bracket commutator roll drift. |
| `pawn-avoidance-and-steering.md` | Lane A/D (Simulation & Presentation) | **done 2026-09-19** | high | Soft occupancy in sim with small dynamic path bias; zero-dynamic grid-dirtying preserves D4 region caching; view-side lateral steering in `PawnPose.Of` via mutual right-hand rule ($\pm 0.6$ m within 2.5 m cell width); smooth Hermite anticipatory offset curve; zero sim footprint, zero hash impact. |
| `e-08-animal-fbx-inspection.md` | Ground, animals unit | **done 2026-09-22** | high (read from the files) / medium (Quaternius attribution) / low (imported size) | Owner-supplied `Pig.fbx` and `Rat.fbx`: Blender 2.79 quadrupeds on one bone convention, no UVs, two flat materials each; the rat has Idle/Walk/Run/Jump/Attack/Death, **the pig has Idle and Jump only** so it cannot walk without a computed gait; sim has no animal, no species and no health. |
| `animals-interview.md` | Phase 1, animals unit | **complete 2026-09-22** (interview conducted) | high | CC0 so committed under `Assets/Art/Custom/`; pig wild first then livestock; rat is vermin, game, threat and the walking proof; MVP is spawn-wander-draw-click from the debug menu only; computed trot for the pig; rats climb anything, pigs stairs and hops; the health model is the unit after. |
| `animals-tab-interview.md` | Phase 1, the Animals tab | **complete 2026-09-23**; brief written to `docs/reference/mockups/animals-tab-brief.md` | high | Nine questions with recommendations for a Claude Design brief: design the F5 and F6 pair together; livestock, pack and guard roles; a wildness and tameness model with three training ticks; allowed areas before pens; animals named on taming; the Work tab's table shape; the inspect pane for a tamed animal; the tokens, fonts and mockups as inputs; static HTML mockups back. |
| `animal-generation-interview.md` | Phase 1, animal generation unit | **complete 2026-09-23** (recommendations adopted; built as design 30) | high | Seven questions with recommendations: a wildlife table per world listing only the two species with art; seed to a density target and top up from the board edge; about one animal per 1,500 surface cells with a per-world ceiling under the 64-figure budget; hog sounders and solitary rats away from the start; nocturnal flag, no breeding; the Animals panel built as RimWorld's Wildlife half on F2; generation then menu, both after PR #167. |
| `a-09-animals.md` | Phase 2, Lane A | **done 2026-09-22** | medium (race/kind, arrival) / high (wildness) / low (wander numbers) | Two layers: a species record (what it is: size, life stages, diet, movement) and a kind record (what spawns: threat weight, ecosystem weight, commonality); flee is an override branch, not a leaf case; wild animals cannot pass doors — the one precedent for a per-species terrain gate; no vertical axis anywhere, so the climbing rule is ours. |
| `a-10-melee-combat.md` | Phase 2, Lane A, item 10 (asked 2026-09-23 for the combat line) | **done 2026-09-23** | high (hit/dodge tables, auto-undraft, unarmed) / medium (attack choice, revenge) | Drafting interrupts any job, holds position, ignores needs and undrafts after 10,000 idle ticks; adjacent enemies are engaged unprompted. Hit 50/80/90 % at melee 0/10/20, dodge 0/10/30 % on the defender's skill; cooldown-paced swings; blunt stuns, sharp bleeds; unarmed ≈ 4 damage / 2 s; weapons 4–10 / 1.5–2.5 s; animals roll a per-species revenge chance, tripled in melee. |
| `synty-sword-combat.md` | Lane E, owner-supplied pack (2026-09-23) | **done 2026-09-23** | high | The Sword Combat pack, unpacked into the shared `Assets/Synty` with GUIDs kept (its C# tool left out). Polygon set is Humanoid and retargets to our colonists; every attack is pre-cut into wind-up / hit / follow-through, so the impact frame is authored. Hit react, stagger, dodge (roll), stun, knockdown loop (= downed) and death with a held pose (= corpse). No punch, no quadruped, two Generic clips unusable. |
| `c-procedural-quadruped-gait.md` | Phase 2, Lane C | **done 2026-09-22** | high (phases, Froude) / medium (amplitudes) | A lateral-sequence walk (0/90/180/270°, duty 0.65), FK sines at hip ±25° and knee ±35°, bob ±2–3 cm at twice stride rate; a 1 m/s pig is well below the trot threshold; FK over IK for a stylised figure at 20–40 m. |
| `meadow-interview.md` | Owner interview (feature), Meadow overhaul | **complete 2026-09-24** | high | Twenty answers in five rounds: a smooth skin over the unchanged layers, ~8 layers of hills (board depth measured first), Meadow meshes through our own shader, lush grass on every tile, all-Meadow trees with more species, no occlusion culling, Ultra 60 fps at 4K and Low 60 fps on an RTX 3050/3060 laptop. Design `38-meadow-overhaul.md`. |
| `e-09-meadow-mesh-data.md` | Lane E (Synty fit), Meadow overhaul | **done 2026-09-24** | high (the data) / medium (wind formula, grass LOD membership, card shape) | Every Meadow LOD child and FBX node is identity with no negative scale, so **one matrix per plant draws every part of every level**. Keep LOD0, the last pre-card mesh and the card; leaf/trunk split on vertex colour B; wind bend = 2R, flutter = B x G. Never instance the 134–209k-triangle patches. |
| `d-16-shader-warmup.md` | Lane D (Unity), Meadow overhaul | **done 2026-09-24** | medium | The player starts on **DX11**. Measure first (Log Shader Compilation + the perf trace); if there are hitches, a `GraphicsStateCollection` traced per API and committed, warmed progressively (on DX11 it is `ShaderVariantCollection.WarmUp`); always two or three curtain frames behind the loading screen. The cyan placeholder is editor-only. |
| `d-17-terrain-skin-shading.md` | Lane D (Unity), Meadow overhaul | **done 2026-09-24** | medium (no timing measured) | One hand-written HLSL material for every chunk, two `Texture2DArray`s filled on the GPU by `CopyTexture`, four layer indices per quarter-cell quad (the most a cell grid can touch) with a one-layer path, luminance height-blend, world-XZ at a 4 m repeat plus macro noise. Detail 512 / **1024** / 2048. |
| `d-18-dense-foliage-cost.md` | Lane D (Unity), Meadow overhaul | **done 2026-09-24** | medium (budgets are estimates) | **Corrected 2026-09-24:** its "drawn up to six times" holds for trees, not for grass, which casts no shadows and sits in queue 2501 outside the prepass; measured at ~1 ms at 4K (design 38 §13), so its depth-priming recommendation is dropped. Still standing: medium clumps, rank-sorted, shrink fade; CPU per-chunk culling (GPU-driven only past ~100k visible); the GPU Resident Drawer does not apply. Budget 2.0 ms at 4K Ultra, 1.5 ms at 1080p Low. |
| `d-19-submission-at-scale.md` | Lane D (Unity), Meadow look pass | **done 2026-09-24** | high (direction) / medium (per-call figures, expected savings) / low (BRG variant cost) | Measured in one world at 4K: the look adds 3.3–3.7 ms, only **0.85 ms** of it CPU submission — the rest is the dressing's fill; **shadows are the largest term (~4.3 ms)**, half of it the cull's all-directions shadow margin (26 → 104 chunks). Ranked: sweep the shadow margin sun-ward; dressing and tufts to `RenderMeshIndirect` + compute cull; levels of detail on the dressing; BRG long-term. Baking rejected (Read/Write, memory, no per-instance fade). |


**Lane G is the UI line of work** (`docs/design/ui-plan-reconciliation.md` sets the division of
labour). Its outputs are design `09`, `10`, `11`, ADRs 0003, 0004, 0006, 0007, the mockups and the
generated content wiki in `docs/wiki/`. Two of its findings matter outside its own lane: layer
question 9's above-and-below policy is decided (x-ray by default, nothing above the slice
clickable), and the icon contract in `09` §7 was wrong on the engine's atlas rules and is now
corrected. **Lane B's Going Medieval finding was absorbed** into ADR 0006 rather than overriding it:
the complaint there is about clickable ghosts, which is an input-routing rule, not a rendering one.

Related, outside this folder: `docs/brief.md` (the governing brief), `docs/reference/screenshots/README.md` (reference-image descriptions), and — new on 2026-09-15 — the Phase 3 design set in `docs/design/` (`00-vision`, `02-world-and-layers`, `03-systems-catalogue`, `04-data-model`, `05-ai-and-jobs`, `06-rendering-and-camera`, `07-modding`, `08-milestones`), the ADRs in `docs/adr/`, and the execution plan `docs/plans/vertical-slice.md`.

**Ownership note for parallel sessions:** this index is edited by the coordinating session only. Research subagents write their own file and nothing else — concurrent edits here produced duplicate rows in wave 1 and had to be reconciled by hand.

## Naming for Phase 2 files (so a fresh session can predict paths)

- Lane A, RimWorld mechanics: `a-01-pawns.md` … `a-16-modding-architecture.md`, numbered as in brief §5.
- Lane B, prior art: `b-going-medieval.md`, `b-dwarf-fortress.md`, `b-timberborn.md`, `b-stonehearth.md`, `b-oxygen-not-included.md`, `b-odd-realm-songs-of-syx.md`.
- Lane C, reference code: `c-cataclysm-dda.md`, `c-luanti.md`, `c-goblin-camp.md`, `c-unity-colony-sims.md`, `c-utility-ai.md`.
- Lane D, Unity spikes: `d-01-architecture.md` … `d-08-linux-ci-tooling.md`.
- Lane E, Synty fit: `e-01-module-mapping.md`, `e-02-characters-animation.md`, `e-03-other-packs.md`, `e-04-tint-strategy.md`, `e-05-character-customisation.md`.
- Lane G, UI and information design: `g-01-ui-information-design.md`, `g-02-unity-ui-framework.md`.
- Lane F: `f-prior-art-here.md`.

Owner's note (2026-09-15): a **separate agent session is researching the interface/UI in depth**. UI and information-design questions (camera/layer UI, overlays, depth cues — parts of Lane B's UI angle and the future `06-rendering-and-camera.md`) are owned there; this line of work must not duplicate them. Its plan has been shared: this project adopts its sim→UI contract as a Lane D1 constraint, and the merge-time corrections it will need (ADR renumbering to 0003/0004, cell size now fixed, project now existing, branch targets) are recorded in `docs/design/ui-plan-reconciliation.md`.

Owner's note for Lane A (2026-09-15): the GitHub organisation `github.com/RimWorldMods` is supplied as a reference for understanding how RimWorld works — **understanding only, nothing is used from it**. The clean-room rule holds: mechanics, formulas, data shapes and design intent may be learned; no code, Def XML, names or flavour text may be copied, and any decompiled game source found there must never enter this repo (brief §1).

Owner's note for Lane E (2026-09-15): Blender is available as a gap-filler. Prefer Synty assets; use Blender only for modules missing at the cell size, UV or atlas fixes, and animation retargeting. `e-01-module-mapping.md` should list each gap with the chosen fix: Synty piece, Blender-made piece, or primitive placeholder.

Each Phase 2 file ends with a short section answering whichever of the twelve layer questions (brief §5) it touches.
